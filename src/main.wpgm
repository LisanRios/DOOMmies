import wollok.game.*
import world.*
import graphics.*
import player.*
import enemigos.*
import objects.*
import gui.*

program main {
	game.width(15)
	game.height(15)
	
	world.generateWorld(8)
	
	var player = new Player()
	var escopeta = new Escopeta(municionUtilizable = 100000)
	var espada = new Espada(municionUtilizable = 100000)
	var fusil = new Fusil(municionUtilizable = 100000)
	var botiquinp = new BotiquinP()
	var botiquinm = new BotiquinM()
	var botiquing = new BotiquinG()
	game.addVisual(player)
	
	gui.player(player)
	gui.init()
	
	game.start()
}